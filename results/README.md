# Results

Each file holds a `summary` block and the per-turn `rows` behind it, so every
number here can be recomputed from the raw judgements.

## Valid

| file | what it is |
|---|---|
| `instruct_dev_epoch{1,2,3}.json` | dev sweep for checkpoint selection |
| `thinking_dev_epoch{1,2,3}.json` | dev sweep for checkpoint selection |
| `instruct_test_best_gold.json` | selected checkpoint, test, gold history |
| `instruct_test_best_free.json` | selected checkpoint, test, free-running |
| `thinking_test_best_gold.json` | selected checkpoint, test, gold history |
| `thinking_test_best_free.json` | selected checkpoint, test, free-running |
| `instruct_test_base_gold.json` | untrained Instruct baseline |
| `FINAL_REPORT.md` | generated by `scripts/analyze.py` |
| `pipeline_report.json` | raw pipeline output, point estimates only |

## Invalid — kept deliberately

**`thinking_test_base_gold.json` — do not cite this file.**

It reports 0.6% hidden pass, which measures a bug in the evaluation config, not
the model. The untrained Qwen3-Thinking reasons at length (Qwen suggests a 32k
output budget), and this run capped generation at 384 tokens, so **100% of its
generations were truncated before reaching an answer** and only 1.9% parsed as
valid JSON.

**There is currently no valid untrained-Thinking baseline at all.**

A 2048-token greedy re-run was abandoned rather than shipped: it inherited the
same two defects -- greedy decoding, and a budget 16x below the card's
recommendation -- and would have produced another number needing the same
caveat.

A properly configured run (the card's `temperature=0.6, top_p=0.95, top_k=20`
with the recommended 32,768-token budget) did not finish a single batch of 4
prompts in 13 minutes, projecting to roughly 10 hours for the full 160-turn
test set on this hardware.

What IS measured, and is the more interesting result: at an identical
384-token budget on identical prompts, the untrained model truncated **100%**
of generations (1.9% valid JSON) while the fine-tuned model truncated **0%**
(97.5% valid JSON). A large part of what SFT bought was the ability to
terminate, not better regexes.

The file is kept rather than deleted because the failure is instructive: a
baseline that is silently measuring your own token limit looks exactly like a
model that cannot do the task. The `truncated` metric is what exposed it, which
is the argument for logging it alongside accuracy.

## A note on reading the tables

`pipeline_report.json` and the per-move table report point estimates with no
uncertainty. Several cells have n=6, where a single item moves the rate by
16.7 pp and a 6/6 result carries a 95% interval of [61, 100]. Use
`FINAL_REPORT.md`, which attaches Wilson intervals and paired McNemar tests and
marks cells too small to interpret.
